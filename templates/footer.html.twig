<div class="footer">
    <ul class="nav-icons">
        <div class="nav-icons__block-left">{{ 'now' | date('Y') }} &copy; Lê Hiếu</div>
        <div class="nav-icons__block-right">{{ 'now' | date('Y') }} &copy; Lê Hiếu</div>
        <li class="nav-icons__item">
            <div class="nav-tooltip">
                <a href="https://github.com/lehieugch68" target="_blank"><i class="fab fa-github"></i></a>
                <span class="nav-tooltiptext">Github</span>
            </div>
        </li>
        <li class="nav-icons__item">
            <div class="nav-tooltip">
                <a href="{{ asset('/') }}"><i class="fas fa-home"></i></a>
                <span class="nav-tooltiptext">Home</span>
            </div>
        </li>
        {% if app.request.session.get('userid') == null %}
            <li class="nav-icons__item">
                <div class="nav-tooltip">
                    <a onclick="openModal('loginform')"><i class="fas fa-sign-in-alt"></i></a>
                    <span class="nav-tooltiptext">Sign In</span>
                </div>
            </li>
        {% else %}
            <li class="nav-icons__item">
                <div class="nav-tooltip">
                    <a href="{{ asset('manager') }}"><i class="fas fa-paw"></i></a>
                    <span class="nav-tooltiptext">Manager &amp; Creator</span>
                </div>
            </li>
            <li class="nav-icons__item">
                <div class="nav-tooltip">
                    <form id="logout" action="{{ asset('logout') }}" method="post">
                        <input type="hidden" value="{{ app.request.uri }}" name="referer">
                    </form>
                    <a onclick="document.getElementById('logout').submit()"><i class="fas fa-sign-out-alt"></i></a>
                    <span class="nav-tooltiptext">Sign Out</span>
                </div>
            </li>
        {% endif %}
    </ul>
</div>